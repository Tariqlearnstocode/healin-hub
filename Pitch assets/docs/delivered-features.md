# The Healin Hub — Delivered Features Log

**Purpose:** A running, plain-English record of everything shipped on the build — big and small — so nothing gets lost before it's shown to Dawn. Some of these look minor in a list but represent real craft (polish, edge cases, trust signals) that's easy to forget happened once it's live.

Format: what shipped · why it's worth mentioning to Dawn. App repo: `~/Coding/healin-hub-app`.

---

## Delivered (as of 2026-07-23)

### Foundation
- **Production database with real geo-search.** Supabase Postgres with PostGIS: every listing is geocoded on creation and search returns providers within N miles sorted by actual measured miles. *Worth mentioning to Dawn:* this is the direct, verified fix for the one thing V1 couldn't do. Tested end-to-end: "60619" put her own med spa 0.2 miles from the searcher.
- **Free geocoding with caching.** City/zip lookups use OpenStreetMap and cache in the database, so there's no Google Maps bill and no API key to misconfigure — the exact failure that killed V1's search. *Worth mentioning:* her running costs stay low and the core feature can't silently break the same way twice.
- **Security baked in (row-level security).** The database itself enforces who sees what: public sees approved listings only, owners edit only their own, Dawn's admin role sees everything. *Worth mentioning:* V1 was compromised by spam injection; this build's permission model lives in the database, not in plugin settings.

### Consumer experience
- **Search that never lands blank.** No filters = everything nationwide, category alone works, location alone works, both combine. Paginated, with a national map fallback. *Worth mentioning:* users can't hit a dead end; every path shows providers.
- **Search autocomplete with service-level filtering.** The What field suggests all categories and services as you type; a picked suggestion becomes a real filter ("Lymphatic Drainage Massage" returns providers offering that exact service, not name matches).
- **Live map with brand pins + mile badges** beside results, on search and on every listing profile.
- **Trust-led homepage copy, grounded in research.** Researched real consumer pain (fear of unlicensed injectors, Instagram-as-search, unverifiable credentials) and wrote the homepage around it: "Self-care you can actually trust." *Worth mentioning:* the site sells what her customers are actually afraid of, not what the tech does. ICP documented in `healin-hub-app/docs/icp.md`.
- **Animated hero map demo.** The homepage plays a scripted search (pins dropping with mile badges, rotating Chicago/Atlanta/Houston) so visitors see the product working before reading a word.
- **Rich listing profiles.** Photo gallery, services chips, weekly hours table, social links (Instagram/Facebook/TikTok), map, call and website buttons, and a lead form that routes messages to the provider. *Worth mentioning:* providers get real leads from day one, which is her pitch for getting them to claim listings.
- **Photo uploads, built to scale.** Up to 5 photos per listing; the first is the cover shown on search results. Photos are compressed in the browser before upload (a 6MB phone photo becomes ~250KB) into two renditions (display + thumbnail), served with 1-year cache headers. The 5-photo cap is enforced by the database itself. *Worth mentioning:* photo storage adds effectively $0 to running costs even at 10,000 listings, because the sizing discipline is built in, not hoped for. Verified end to end: upload, cap rejection of a 6th photo, gallery, and search-card thumbnails.
- **Report-a-listing.** Every profile has a report link (wrong info, possibly unlicensed, closed, spam) feeding an admin moderation queue. *Worth mentioning:* "possibly unlicensed" reporting is the trust promise with teeth; the community helps police the checkmark.
- **Fully responsive.** Checked at phone width, issues fixed.

### Provider side
- **Submit-a-listing with automatic geocoding + review queue.** Listings enter as pending and can't exist without coordinates. Providers pick their exact services (checkboxes feed service search), set hours, and add socials in the same form.
- **Claim-a-listing** with evidence + admin approve/transfer. *Worth mentioning:* the claim flow is what the $100 outreach add-on drives providers into.
- **Provider dashboard.** Manage/edit listings, claim status, and a client-inquiry inbox showing each lead's message with tap-to-email/call contact.
- **Full account flows.** Email/password signup with confirmation callback, sign-in, password reset (request link → emailed → choose new password). No dead ends for real users.

### Admin (Dawn's control panel)
- **Moderation.** Approval queue, claim resolution, report queue (resolve/dismiss), ✓ Vetted and Featured toggles, edit/remove any listing.
- **Filtering and sorting at scale.** Listings table filters by name/category/state/status, sorts on any column, 50 per page, all in the database query. *Worth mentioning:* when the directory is 5,000 listings, her control panel already handles it.
- **Taxonomy management.** Add/edit categories, add/remove services per category, from the UI.
- **SEO location management.** Add a city (auto-geocoded) and ~11 landing pages + sitemap entries publish instantly; remove one to unpublish. No developer needed.
- **User management.** Promote/demote admins, see each user's listings count. Guard: an admin can't demote themselves.
- **Editable page copy (CMS-lite).** Override any landing page's title, meta description, intro, or body; blank fields fall back to templates. *Worth mentioning:* Dawn can strengthen her best-performing pages herself, forever, without paying for edits.

### SEO engine
- **110+ page SEO footprint from two route files.** Category pages (`/med-spas`), city pages (`/chicago-il`), and category-in-city pages (`/med-spas/chicago-il`) render live from the database with structured data (LocalBusiness/ItemList), canonicals, cross-links, and an auto-generated sitemap. *Worth mentioning:* directories live or die on organic search; this is the growth engine, and it scales by adding data, not code.

### V2 readiness (built, not switched on)
- **Schema for reviews/ratings, paid tiers (free/premium/featured), provider analytics events, and favorites.** *Worth mentioning:* the $1,500 Quote B upgrade path is genuinely "flip on features," not "rebuild," exactly as sold on the call.

### Polish
- **Branded 404 page** (aubergine/teal, animated map-pin, clear paths back).
- **Supporting pages:** About, Contact, Pricing ("list free," honest premium-later note), Privacy, Terms (frames "Vetted" as a review process, not a guarantee — a real liability point in this space).
- **Categories index page** with live listing counts and per-category service lists.
- **Dawn's real logo everywhere:** header, footer, white variant staged for dark surfaces. (Favicon was still the Next.js default at the time of this note; fixed 2026-07-25, see below.)

### Audit hardening (2026-07-24, from the end-to-end audit)
- **Repo now rebuilds the real database.** The four migrations that existed only in the live project (auto profile on signup, V2 schema + CMS tables, reports, photo storage) are committed, so a fresh provision matches production.
- **Closed two privilege-escalation holes.** Database triggers now stop a signed-up user from making themselves admin and stop a provider from self-approving or self-awarding the ✓ Vetted badge via direct API calls. *Worth mentioning:* this is the trust model the whole brand rests on.
- **Search matches services, not just names.** "Lip filler" or "facial" now finds providers whose services match even when their business name doesn't.
- **Listing pages got per-page titles and LocalBusiness structured data** (a contractual SEO item), plus two XSS fixes (structured-data injection, unvalidated provider URLs) and a timeout on the geocoder call.
- **Marketing copy aligned with the Terms.** "Verify/checked by hand/safe" softened to review-process language so the homepage can't be quoted against the disclaimer.
- **Mobile hamburger nav** — the header no longer overflows on phones, the single most visible mobile break.
- **Accessibility pass:** visible keyboard focus rings everywhere, animations respect the OS reduced-motion setting, low-contrast placeholder text fixed.
- **Lead + report forms hardened against spam** (V1's killer): hidden honeypot field, server-side validation, and database rules that only accept submissions against approved listings.
- **Search now sorts purely by real miles**, matching the site's own promise. A featured listing 90 miles away no longer outranks a vetted one nearby.
- **Full polish pass (rest of the audit backlog):** optimized images, loading skeletons on every page, results map shows per-pin mile badges + a "you are here" dot, admin queries aggregate in SQL (holds at 5,000+ listings), confirmation dialogs on deletes/role changes, restore button for removed listings, edit forms locked to the listing's owner, admin area gated at the routing layer, one consistent CTA everywhere.
- **v2 audit round (8-agent re-audit, same day):** every Quote A feature independently re-verified as delivered. Fixed: CMS long-form copy now renders formatted (was showing raw Markdown), claim flow can no longer transfer an already-owned listing, login redirect hardened against phishing, admin approval queue shows full details + submitter contact before approving, mobile badge rendering glitch, and three database-level integrity fixes. Copy over-claim on the homepage left for Dawn's copy pass.
- **Launch photos for all 10 demo listings (2026-07-25).** Curated 20 licensed stock photos (Pexels free license, hand-reviewed for category fit), converted to the app's WebP display + thumbnail renditions, uploaded to storage, and wired to every listing. *Worth mentioning:* search results and profiles no longer look empty on first visit; Dawn can replace them with real provider photos anytime through the normal photo manager.
- **Real favicon on both the app and the pitch site.** The app was still flying the default Next.js logo in browser tabs, bookmarks, and link previews; the pitch site had no icon at all. Both now use a simplified brand mark (plum disc, teal rim, white H) at 16/32/48px, plus a 180px home-screen icon for iOS. The full globe mark loses its detail below about 32px, so the small mark is a deliberate reduction rather than a shrink.
- **Share cards for every page (Open Graph + Twitter).** Before this, a link to the site pasted into Instagram, iMessage, Facebook or Slack unfurled as a bare URL with no image and no title. Now every page produces a branded 1200x630 card in Dawn's own colors and fonts, and listing pages generate their own: the provider's name in the serif, her category, her city, and a teal "Vetted" pill if she's vetted. *Worth mentioning to Dawn:* this is the piece that makes providers sharing their own listing page do marketing work for the directory. Also set `metadataBase`, which OG images need to resolve to real URLs in production.
- **Tier control in the admin portal (2026-08-01).** Dawn can now set any listing to Free / Premium / Featured straight from the admin table — a dropdown that saves on change, protected so only admins can touch it (a database trigger reverts anyone else's attempt). No billing attached yet by design: it's pure CMS control so she can hand-pick placements before payments exist.
- **Social links expanded from 3 networks to 8 (2026-08-01).** Listing forms and profiles now support YouTube, LinkedIn, X, Pinterest, and Yelp alongside Instagram, Facebook and TikTok, with proper labels ("TikTok", not "Tiktok") everywhere they render.
- **Listing profile page redesigned (2026-08-01).** Breadcrumbs (Home → Browse → Category → name), a two-column desktop layout with a sticky "Get in touch" card, carded sections with icons, labeled lead-form fields, an adaptive photo gallery (1, 2, or mosaic layouts), and a "More in this category nearby" strip that cross-links providers. *Worth mentioning:* the profile went from a bare centered column to an actual directory experience.
- **100 real Chicago-area providers researched and seeded (2026-08-01).** Ten research agents (one per category) found 10 currently-operating businesses each across Chicago and the suburbs — name, address, phone, website, socials, hours, and services, each with source URLs recorded, descriptions written fresh (not copied). Seeded as approved / not-vetted / free tier, so the ✓ Vetted badge stays Dawn's call. *Honesty note:* details came from the businesses' own sites at research time; a human spot-check before outreach is still wise.
- **Demo listings retired; Chicago Dollz made real (2026-08-01).** The 9 fabricated demo listings (555 numbers) are soft-removed (restorable from admin). Chicago Dollz Experience — Dawn's own studio — now carries its verified details from chicagodollzexperience.com: 1845 W 95th St, real phone/email, actual hours, socials, and services (post-op massage, body contouring/sculpting). It keeps its Vetted + Featured badges.
- **Labeled stock photos for photo-less listings (2026-08-01).** The ~90 researched providers have no photos yet, so profiles and search cards fall back to a licensed per-category stock image with an explicit "Stock photo" badge (profiles) / "STOCK" tag (cards). The moment a provider adds real photos, the placeholder disappears.
- **Chicago Dollz photos: tried real, reverted to stock (2026-08-01).** With Dawn's permission we pulled four images from her own site through the normal photo pipeline, but on review they read as generic marketing stock rather than her actual studio, so Tariq called it back to the original two curated stock photos. The converted website images remain in storage unused; the real win here will be actual studio photos from Dawn, uploadable anytime via the photo manager.
- **Footer rebuilt with SEO structure + sitewide schema (2026-08-01).** The old footer was a logo, one line, and four links. Now it's a proper directory footer: a provider CTA band ("Own a med spa or studio?" → List your business), four link columns (all 10 categories, 8 city pages with varied anchors like "Botox in Atlanta", provider links, company links), a crawlable entity statement, and a copyright/legal bar. Also added Organization + WebSite JSON-LD (with a SearchAction) to the root layout so every page identifies the brand to Google and AI answer engines. Suggestions came from a three-agent pass (UI/UX, SEO/GEO, copywriting) synthesized into one build.
- **Services vs. categories scoped (2026-08-01).** Dawn's site lists ~12 services; her listing can only show 3 curated tags — we're conflating the browse taxonomy with a provider's actual menu. Wrote a scoping doc (`healin-hub-app/docs/services-menu-scope.md`) proposing per-listing "Services & pricing" menus (free-form items, optional prices/durations, owner-editable, ~2 days of work) while keeping the curated taxonomy for search. Includes three pricing-display questions for Dawn. Built same day — see below.
- **Real studio photos on Chicago Dollz (2026-08-01, from Tariq).** Five actual photos — storefront on 95th, Dawn in the branded hallway, the hyperbaric oxygen room, the waiting lounge, and a CryoSlimming treatment — replaced the stock pair through the normal photo pipeline. Her profile is now 100% real: data from her site, photos from her studio.
- **Per-listing service menus built (2026-08-01).** The scoped feature, shipped: providers (and admins) get a "Services & pricing" editor on the listing edit page — free-form service names, optional price / "from"-style note / duration / description, up to 30 items, remove checkboxes, no JS tricks. Profiles render it as a priced menu with the curated tags beneath. *Live as of 2026-08-05:* migration `0013_listing_menu_items.sql` is applied to the production database (`listing_menu_items` table confirmed present), so the service-menu UI is switched on.
- **Brand icons on social links (2026-08-01).** The Follow pills now show each network's actual logo (Instagram, Facebook, TikTok, YouTube, X, Pinterest, Yelp via the simple-icons package; LinkedIn glyph inlined since that package dropped it), with a generic link icon fallback so unknown networks never break.
- **Admin + provider dashboards redesigned (2026-08-05, from Tariq).** Fixed the "weird margins": every admin page (main console, Users, Categories & locations, Page content) and the provider dashboard now share the site header's `max-w-6xl` container, so content lines up under the logo instead of floating narrower — they were mismatched (5xl / 4xl / 3xl) and even jumped narrower once the loading skeleton (already 6xl) resolved. The listings and users lists became real carded tables: shaded uppercase header row, hairline dividers, row hover, tabular dates, and a dedicated right-aligned Actions column so the date no longer collides with Edit/Remove. Added a shared `StatusBadge` component so an admin's "pending"/"approved" reads identically to the provider dashboard's status pills. Filter and page-picker rows are now grouped toolbars, and taxonomy's add-forms got labels + dashed "add" affordances. Pure UI/UX — no data or behavior changes.
- **Photo uploads were broken for every provider — root-caused and fixed (2026-08-05).** Providers hit a raw "new row violates row-level security policy" error on the listing photo uploader, and it turned out no real provider upload had ever worked. The storage policy from migration 0006 wrote `storage.foldername(name)` intending the uploaded file's path, but that reference sat inside a subquery over `listings`, and `listings` also has a `name` column, so Postgres bound it to the business name ("Tariq's Fillers") instead of the storage path. The folder check always came back empty, so every owner upload was denied and only service-role seeding (the stock photos) got through. Fixed by qualifying the reference as `objects.name` (migration `0014`), applied to the live Supabase project the same day. Owner uploads and deletes now work.
- **Photo errors no longer leak database internals (2026-08-05).** Even with the policy fixed, the uploader used to dump raw Postgres error strings into the UI. It now maps permission/RLS failures to a friendly "your account may not have permission yet" message and everything else to a generic retry, logging the real error to the console for debugging.
- **Address autocomplete on the listing form (2026-08-05).** Providers had to type street, city, state, and zip into four separate boxes by hand. Now a single search field (powered by Photon, the free OpenStreetMap typeahead — no API key, no billing) fills all four on pick and captures the exact coordinates so the listing skips re-geocoding. Manual entry still works as a fallback.
- **Hours are dropdowns now, not free text (2026-08-05).** Each day was an open text box, which is easy to fill inconsistently. It's now an Open / Closed / By appointment selector plus 30-minute open/close time pickers, composed back into the same stored string format so nothing downstream changed.
- **Signup subtitle corrected + provider CTA cleaned up (2026-08-05).** The signup page claimed it was for "clients who want to save favorites," but there's no favorites feature — reworded to provider-only. And signed-in providers no longer see the "List your business" CTA in the header, mobile nav, or footer band, where it made no sense once they already have a dashboard.
- **Admin storage tracker (2026-08-05, from Tariq).** New "Storage" tab in the admin console showing how full the `listing-photos` bucket is against the 1 GB free-tier cap — a usage bar (teal, amber at 75%, red at 90%), total used, and file count — so we see it coming before uploads start failing. Backed by an `admin_storage_usage()` RPC (migration `0015`, applied live 2026-08-05) since the storage schema isn't queryable through the normal API. Currently ~3.5 MB across 62 files.
- **Unbroke the production build (2026-08-05).** `main` had a pre-existing TypeScript error (an implicit-`any` in the admin page-content editor) that fails `next build`, which meant Vercel deploys were silently failing. Fixed with a one-line type annotation so today's work could actually ship.
- **Address autocomplete upgraded: Photon → Geoapify → Mapbox (2026-08-05).** The original Photon (OSM) typeahead ranked results globally with no location bias, so a partial like "16554 wyoming" surfaced Aurora, CO instead of Detroit. Swapped to Geoapify with a MI/IL proximity bias + US filter (Detroit now ranks first), but it still felt laggy in production — both Photon and Geoapify are OpenStreetMap-backed. Moved to Mapbox Geocoding v6 (commercial data, faster edge delivery), kept the MI/IL bias, and tightened the debounce 300ms → 180ms so typing feels snappier. One forward-geocode call returns coordinates plus a 2-letter state code, so street/city/state/zip autofill and the geocode-skip still work; manual entry remains the fallback. Needs `NEXT_PUBLIC_MAPBOX_TOKEN` (URL-restricted public token) in Vercel.
- **Email notifications wired for all three form funnels (2026-08-05).** Leads, listing claims, and listing reports now send an email the moment they're submitted, so nobody has to poll the admin dashboard to know something came in. All three route to `hello@thehealinhub.com` for now (a lead's reply-to is set to the client, a claim's to the provider, so hitting Reply writes straight back to them). Built on Resend via a single `fetch` in `src/lib/mail.ts` — no SDK, and best-effort: if the key is missing or the API errors, the form still succeeds and the row still lands in the database, so email can never block a submission. *Worth mentioning to Dawn:* the data was always being saved; this is the missing "someone actually gets pinged" half. **Dormant until launch** — it needs `RESEND_API_KEY` set once her domain is verified (see to-do below); until then it logs "skipped" and saves as before.
- **Search "Where" filter: adaptive point-vs-region + exact pagination (2026-08-05).** The search location box was one free-text field ("city, state, or zip") feeding a Nominatim geocode + radius search — ambiguous, and it treated a whole state as a point (distance from a state's centroid is meaningless). Now it's a Mapbox autocomplete (`PlaceAutocomplete`): you pick a city/zip/state from a dropdown, and the search adapts to what you picked — a city or zip does the distance/radius search (radius chips shown, sorted by miles); a state filters to every provider in that state (radius hidden, sorted Featured → Vetted → City A–Z). The heading and map re-center per mode. Typed-through queries (no pick) are classified server-side by a Mapbox lookup that reads the feature type, so a bare "michigan" still routes to the state filter. Pagination dropped to 10/page with real totals — "Page X of Y" (exact SQL count for the state/nationwide lists; the distance RPC fetches a capped set and pages in memory). Map tiles stay on free OpenStreetMap/Leaflet — only geocoding uses Mapbox.
- **Teal-led brand rebalance across the whole app (2026-08-06).** The site led with deep aubergine/purple; the client wanted teal (the logo's third color) out front and a lighter, more gradient-forward feel — without losing Dawn's purple. Reworked the palette *roles* (the color scales themselves are unchanged): teal now leads every light ground, eyebrow, heading emphasis, link, active/selected state, button (teal-600, so white text passes AA — not the lower-contrast teal-500), focus ring, map pin, and loading skeleton. The home and for-providers heroes are now airy teal/orchid radial-gradient grounds with ink text instead of flat purple blocks; the closing CTA bands are deep-teal gradients; the footer carries the same hero gradient. Her purple stays as all the body/heading ink, plus a deep-aubergine showcase (the home "For providers" band) and small depth accents (the discovery-form aside, the 404 pin). Mauve is held back to pale tints, lightened orchid-400 decoration, and the single vetted map pin — no saturated orchid-500 fills anywhere. Added deep-teal tokens (teal-800/900) for the anchor gradients. The admin console was left on the old palette (only the global primary-button color swap reached it) since a full admin reskin isn't needed. A standalone reference board is saved at `Pitch assets/teal-direction.html`. Verified: every non-admin route compiles (HTTP 200, no console/build errors), spot-checked home, for-providers, search, and the featured carousel live.
- **Provider notifications now reach the provider, not just the admin inbox (2026-08-06).** From a provider-perspective audit: the email wiring shipped 2026-08-05 sent every lead, claim, and report to the shared `hello@thehealinhub.com` inbox — so the "Client inquiries sent straight to you" promise on the for-providers and pricing pages was only true via the dashboard, which a provider had no reason to keep refreshing. Fixed three routes: (1) **new leads** now go to the provider — their account email if the listing is owned, else the business email on the listing, else the shared inbox for admin-created/unclaimed listings (reply-to still the client, so Reply writes back to them); (2) **listing approved/rejected** now emails the owner (approval links to their live profile; rejection invites them to fix and resubmit) — from both the single-row moderate action and the bulk-approve bar; (3) **claim approved/rejected** now emails the claimant (approval points them to their dashboard; rejection asks for verification). Reports still route to the admin inbox by design. Provider/claimant emails are looked up from `auth.users` via a new best-effort `getUserEmail()` service-role helper in `mail.ts` — returns null (and the send is skipped) when there's no owner or no mail key, so nothing ever blocks a submission. Still **dormant until `RESEND_API_KEY` is set** (same as the original wiring). Typecheck + lint clean.
- **FAQs are now editable from the admin console (2026-08-06).** The `/for-providers` FAQ was a hardcoded list in the page code — changing a word meant a developer and a deploy. Dawn can now edit every question and answer herself from a new **Admin → FAQs** tab: edit copy inline, reorder with up/down, publish/unpublish (hide without deleting), add new questions, and delete. Changes go live on the page immediately. Built on a new `site_faqs` table keyed by a `placement` value, so the same editor can back a homepage/consumer FAQ later with no rework — the provider FAQ is just the first block. RLS matches the rest of the app: only an admin writes, the public sees only published rows. This also closes the open audit item — the FAQ copy that implied a profile goes live immediately is now Dawn's to reword in seconds, no code change. *Worth mentioning to Dawn:* the wording of her most-read sales page is now in her hands. Migration `0016_site_faqs.sql` applied to live Supabase 2026-08-06; the page verified rendering all 5 FAQs from the table with no errors. Typecheck + lint clean.
- **Providers can now preview their own listing before it's live + a beefed-up dashboard (2026-08-06).** From the same provider audit: a submitted listing is `pending` and hard-filtered out of the public site until an admin approves it, so a new provider couldn't see their own profile anywhere — the public URL 404'd. Now the listing page renders a non-approved listing as a **self-preview for its owner (or an admin)** — RLS scopes the row and an explicit `canPreview` check gates it — topped with a status-aware banner ("In review / Not approved / Removed — only you can see this") and an "Edit listing" button. In preview the message form is shown inactive (with a note that it goes live on approval), the claim/report footer is hidden, and no `LocalBusiness` JSON-LD is emitted so a preview can never be indexed. The **provider dashboard** got a small lift to match: an at-a-glance stats row (Listings · Live · In review · Inquiries) and a **View / Preview** link on every listing card (opens the live/preview page in a new tab) next to Edit. Approved public listings are visually unchanged. *Worth mentioning to Dawn:* providers no longer have to guess whether their profile "worked" — they can look at exactly what will publish. Still open from the audit: the `/for-providers` FAQ copy that implies a profile is live immediately (a one-line reword). Typecheck clean; approved-listing render verified (HTTP 200, no console/server errors).

---

## Still to do

- [x] **Apply migrations 0007 (RLS hardening) + 0008 (search text expansion) to the live Supabase project** — applied and verified 2026-07-24.
- [x] **Add `SUPABASE_SERVICE_ROLE_KEY`** to `.env.local` — done 2026-07-24. Still needs adding to Vercel env at deploy time.
- [x] **Apply migration 0013 (`listing_menu_items`)** to the live Supabase project — verified applied 2026-08-05 (table present, service-menu UI live).
- [ ] **Sync the Supabase migration ledger.** 0013, 0014, and 0015 are all applied and working in production, but were run by pasting SQL into the editor, so `supabase_migrations.schema_migrations` has no rows for them. Harmless today, but a future `supabase db push` will try to re-run them and error on "already exists." Fix by inserting their version/name rows into that table.
- [x] **Apply migration `0016_site_faqs.sql`** to the live Supabase project — applied and verified 2026-08-06 (`site_faqs` table present, provider FAQ rendering from it). Run by pasting SQL into the editor, so its row still needs adding to `supabase_migrations.schema_migrations` (see ledger-sync item below).
- [ ] **Turn on email notifications** for leads / claims / reports. The wiring is shipped and dormant (see delivered log). Once Dawn's domain is verified in Resend: (1) verify `thehealinhub.com` in the Resend dashboard, (2) add `RESEND_API_KEY` to Vercel env, (3) confirm the from-address domain — defaults to `notifications@thehealinhub.com`, override with `HEALIN_NOTIFY_FROM` if needed, and `HEALIN_NOTIFY_EMAIL` to change the destination from `hello@thehealinhub.com`. No code change needed — set the env and it starts sending. Then send one test lead/claim/report and confirm the email arrives.
- [ ] **Deploy** to production (Vercel + domain).
- [ ] **Pre-launch config:** point Supabase auth Site URL + redirect URLs and NEXT_PUBLIC_SITE_URL at the production domain (currently localhost). NEXT_PUBLIC_SITE_URL now also drives `metadataBase`, so until it's set the share cards point at localhost and won't render for Facebook or iMessage.
- [ ] **Pitch site social tags.** The app has full Open Graph now; `Pitch assets/` still has none, so the proposal link unfurls bare when it's forwarded. Deliberately deferred 2026-07-25.
- [ ] **Post-launch handoff:** transfer the Supabase project (currently $10/mo on Tariq's account) to Dawn's own Supabase organization so she owns her data and the bill. Remember to move the env keys on Vercel to the transferred project's values if they change.
- [ ] **Swap the map tile host at scale (not urgent).** The Leaflet map pulls tiles from OpenStreetMap's public community server (`tile.openstreetmap.org`), whose usage policy discourages heavy commercial traffic and can throttle/block busy sites. Fine at current traffic. When traffic grows, point the `TileLayer` url in `LeafletMap.tsx` at a dedicated tile host (MapTiler / Stadia / Mapbox raster) — a one-line change, no rearchitecting. Geocoding already uses Mapbox; this is only about the map's background tiles.

---

## How to use this doc

Add one line per shippable thing, as it ships — not at the end of the week from memory. Keep the "why it's worth mentioning" part honest: if it's genuinely minor, say so briefly rather than overselling it. When it's time to update Dawn, this doubles as the source material for a status email or call.
